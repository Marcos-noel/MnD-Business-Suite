# MnD Business Suite - Production Deployment Configuration
# Update these values when deploying on Vercel (frontend) + Render (backend)

# Backend URL (your deployed Render backend)
BACKEND_URL="https://mnd-backend.onrender.com"
NEXT_PUBLIC_BACKEND_URL="https://mnd-backend.onrender.com"

# Cookie settings for production
COOKIE_SECURE=true
NODE_ENV=production
